li.jingle
  = image_tag item.image.file.url(:medium), data: { image: item.image.file.url(:medium) }
  .jingle-content
    .jingle-header
      h4
        = link_to item.title, item, class: 'header', remote: true
    span.price = number_to_currency(item.price, unit: item.price_currency)
